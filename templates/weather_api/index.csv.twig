city,country,date,celsius
{% for measurement in measurements %}
	{{ city }},{{ country }},{{ measurement.date.format("Y-m-d") }},{{ measurement.temperature }},{{ measurement.fahrenheit }}
{% endfor %}
